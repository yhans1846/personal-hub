# Personal Hub 全功能巡检报告

| 项 | 内容 |
|----|------|
| 时间 | 2026-07-15 23:32–23:41；**23:48 已修复并验证** |
| 环境 | :3000 / :8080 · admin/123456 |
| 方法 | Playwright + Browser MCP + 日志 |
| 产物 | `logs/qa-artifacts/full_feature_result.json` · screenshots/ |

---

## 0. 修复回写（2026-07-15）

| 问题 | 处理 |
|------|------|
| P0 工作台/通知深链空白 | `buildEditPath` + `useDeepLinkDialog`；`?create=1` |
| 旧路径 | router `/:id/edit` → `?edit=` |
| P2 编辑器首屏空白 | `initialLoading` 骨架 |
| 阅读标题 / 计划 Plus | `bookTitle`；补 `Plus` |

验证：`?edit=1` 与旧 `/1/edit` 均弹窗；`pnpm test` 8 passed。

---

## 1. 结论摘要

后端正常；列表与笔记编辑/预览可用。**根因：Dialog 模块深链未注册路由 → 空白页。**

| 维度 | 结果 |
|------|------|
| error.log | 0 B |
| app.log | 无 Exception/500 |
| API | 无 ≥400 |
| Console | Router No match（深链） |
| 已注册 16 路由 / 查看全部 | 正常 |
| `/xxx/:id/edit`、`/xxx/new` | 曾大量失败（已修） |
| 笔记 edit/preview | 正常（Vditor 可晚 1–3s） |

---

## 2. 后端日志对照

error.log 空；app.log 可见 JWT/笔记读写/通知/布局 INFO。启动 WARN `UserDetailsServiceAutoConfiguration` 可忽略。  
**空白页不产生后端 ERROR** → 判前端路由。

---

## 3. 模块路由可达性

侧栏直达列表（notes/diaries/readings/study-records/todos/study-plans/bookmarks/files/tags/categories/settings/stats/search/recycle/dashboard）**通过**。

---

## 4. 深链与跳转（修复前失败，后已通）

失败形态：工作台条目、快捷「开始学习」、通知铃铛 → `/xxx/:id/edit` 或 `/xxx/new`。  
正确形态：`?edit=` / `?create=1`；笔记保留独立 `/notes/:id/edit|preview`。

---

## 5. 根因与方案

List+Dialog 无独立编辑路由。方案 A（已落地）：深链改 query + `useDeepLinkDialog` + redirect 兼容。

---

## 6. 优先级（历史）

| 级 | 项 | 状态 |
|----|-----|------|
| P0 | 深链空白 | ✅ 已修 |
| P2 | 编辑器骨架 | ✅ 已修 |

---

## 7. 方法备忘

详见 `CURSOR_BROWSER_TESTING.md`：Browser MCP 顺序、Playwright 脚本、双日志对照。
